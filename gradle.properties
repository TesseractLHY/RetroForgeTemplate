# ------------------------------------------------------------------
# Shared mod metadata. Single source of truth for all modules.
# The Java mirror of these values lives in
#   core/src/main/java/com/example/retroforge/core/Constants.java
# Keep the two in sync (version + id + name).
# ------------------------------------------------------------------
mod_id = retroforge
mod_name = RetroForge Template
mod_version = 1.0.0
mod_description = A multi-version Forge mod template (MC 1.7.10 + 1.12.2) sharing a common core.
mod_authors = YourName
root_package = com.example.retroforge

# Give Gradle enough heap for RFG decompile/deobf work
org.gradle.jvmargs = -Xmx3G
org.gradle.daemon = true
